# 프로젝트 규칙 (GEMINI.md)

이 문서는 PartTimeCat 프로젝트의 기술 문서 작성을 위한 Gemini AI의 작업 규칙입니다. 모든 작업은 아래 지침을 최우선으로 준수합니다.

## 1. 접근 범위 및 제한
- **접근 허용:** `C:\Users\home\Documents\GitHub\PartTimeCat\Assets` 하위 경로에 대해서만 접근합니다.
- **파일 금지:** `.meta` 확장자 파일은 절대 열람하지 않습니다. (GUID 확인 등 불가피한 경우 반드시 사용자 승인 필요)

## 2. 정보 수집 및 분석
- **프로젝트 설명:** 프로젝트 개요는 `Docs/Desc_Root.md`를 우선 열람합니다. 정보가 부족할 경우 즉시 사용자에게 구체적인 질문을 합니다.
- **의존성 파악:** `.cs` 파일만으로 파악이 어려운 경우, 사용자에게 씬 파일 열람을 요청합니다.
  - 요청 형식: "[파일명]의 분석을 위해 씬 파일 열람이 필요합니다. 씬 파일의 이름을 알려주세요."
  - **씬 파일 제한:** `OcclusionCullingSettings`, `RenderSettings`, `LightmapSettings`, `NavMeshSettings` 설정은 열람에서 제외합니다. 
  - **컨텍스트 관리:** 씬 파일 열람 후 분석이 완료되면 즉시 해당 컨텍스트를 제거합니다.
- **캐싱:** AI 판단상 특정 내용의 캐싱이 필요한 경우, 반드시 사전에 사용자 승인을 받습니다.

## 3. 컨텍스트 및 시스템 관리
- **모니터링:** 세션 컨텍스트 사용량이 임계치(약 40%)에 도달했다고 판단될 경우 즉시 사용자에게 알립니다.
- **충돌 대응:** 사용자의 요구사항과 이 문서의 원칙이 충돌할 경우, 즉시 작업을 멈추고 원칙 수정에 대한 논의를 시작합니다.

## 4. 쓰기 작업 및 코드 관리
- **승인 절차:** 모든 파일 생성 및 쓰기 작업 전에는 반드시 사용자에게 계획을 공유하고 승인을 받습니다. (단, `.md` 확장자 파일의 생성은 승인 없이 수행 가능)
- **이슈 기록:** 코드 분석 중 발견된 버그나 개선점은 임의로 수정하지 않으며, `Docs/Risks/ToFix.md` 파일에 기록하여 사용자에게 보고합니다.

---
## 5. 문서화 기법
1. 각 모듈 디렉토리(Assets/_Scripts/<ModuleName>/)의 전체 파일 리스트(하위 디렉토리 포함)를 먼저 사용자에게 점검받습니다.
2. 모든 .cs 파일을 읽고 분석하며, 기존 문서가 존재할 경우 '<ModuleName>_New.md' 형식으로 파일을 생성합니다.
3. 문서 구조: 개요(Overview), Scope, 핵심 로직 컴포넌트, UI 요소, 데이터 및 메시지 정의 순으로 작성합니다.
4. 문서 작성 시 코드 스니펫 대신 자연어 설명을 원칙으로 하되, 코드 요소는 백틱(` `)으로 인용합니다.
5. 문서 작업 시 'Docs/' 하위의 '_New' 접미사를 갖는 이름의 md 파일들을 필수로 열람하여 레퍼런스로 참고합니다.

